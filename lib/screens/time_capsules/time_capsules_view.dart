import 'package:flutter/material.dart';

class TimeCapsulesView extends StatelessWidget {
  const TimeCapsulesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Placeholder(
        child: Text('time capsules view'),
      ),
    );
  }
}
