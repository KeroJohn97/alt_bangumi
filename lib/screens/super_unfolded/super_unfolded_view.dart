import 'package:flutter/material.dart';

class SuperUnfoldedView extends StatelessWidget {
  const SuperUnfoldedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Placeholder(
        child: Text('super unfolded view'),
      ),
    );
  }
}
