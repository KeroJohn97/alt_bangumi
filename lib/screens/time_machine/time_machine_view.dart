import 'package:flutter/material.dart';

class TimeMachineView extends StatelessWidget {
  const TimeMachineView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Placeholder(
        child: Text('time machine view'),
      ),
    );
  }
}
